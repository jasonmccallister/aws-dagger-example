[project]
name = "aws-dagger-example"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.36.14",
    "dagger-io",
]

[tool.uv.sources]
dagger-io = { path = "sdk", editable = true }

[build-system]
requires = ["hatchling==1.25.0"]
build-backend = "hatchling.build"
